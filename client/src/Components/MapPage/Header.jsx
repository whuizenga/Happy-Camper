import React, { Component } from 'react';

class MapPageHeader extends Component {
    render() {
        return (
            <div>
                This is the header of the page. It need a search component and two links.
            </div>
        );
    }
}

export default MapPageHeader;